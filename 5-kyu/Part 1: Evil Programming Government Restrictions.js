const anarchy = function () {
  return
 'I can write numbers like, ' + +!!true + ', ' + (+!!true + (+!!true)) + ', ' + (+!!true + (+!!true)+ (+!!true)) + '.';
}


// clever stuff

let anarchy = function () {
  var one = Math.PI / Math.PI;
  var two = one + one;
  var three = two + one;
  return 'I can write numbers like, ' + one + ', ' + two + ', ' + three + '.';
}
