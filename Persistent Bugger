function persistence(num) {
  let count = 0
  let mul = function (n) {
    let m = n.toString().split('');
    if (m.length<=1) return
    if (m.length>=2) {
      let doidera = m.reduce((a, b) => a * b, 1)
      count+=1;
    mul(doidera);
    }
  }
mul(num)
  return count 
}
