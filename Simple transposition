function simpleTransposition(text) {
  let row1 = '';
  let row2 = '';
  for (let i = 0; i < text.length; i += 1) {
     if (i % 2 === 0) {
       row1 += text[i];
     } else {
       row2 += text[i];
     }
  }
  return row1 + row2;
}
