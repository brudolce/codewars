function cutIt(arr){
 
let min = Math.min(...arr.map(str => str.length))
//... used to expand the function to n arguments
let arr1=[]
 for(i=0;i<arr.length;i++){
    arr1.push(arr[i].slice(0,min))
  }
return arr1
}
